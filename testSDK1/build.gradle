plugins {
    id 'java'
}

group = 'org.example'
version = '1.0.0'

sourceCompatibility = '11'
targetCompatibility = '11'

repositories {
    mavenCentral()
}

dependencies {
    // Add dependencies if needed
}

jar {
    manifest {
        attributes(
                'Implementation-Title': 'HelloWorldSDK',
                'Implementation-Version': version
        )
    }
}
